interface:
  display_name: 拼贴分镜与关键帧
  short_description: 生成完整拼贴分镜与首尾关键帧，并按需制作透明分层素材
  default_prompt: Use $generate-layered-collage 把这段内容制作成完整的拼贴分镜；只有明确需要独立运动或复用时才拆分图层。
  icon_small: assets/icon.svg
  icon_large: assets/icon.svg
policy:
  products:
  - chatgpt
  - codex
  - api
  - atlas
  allow_implicit_invocation: true
